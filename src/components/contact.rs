use yew::prelude::*;

#[function_component(Contact)]
pub fn contact() -> Html {
    html! {
        <section id="contact" class="contactSection">
        {"Contact section"}
        </section>
    }
}
