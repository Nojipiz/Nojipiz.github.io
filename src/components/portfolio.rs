use yew::prelude::*;

#[function_component(Portfolio)]
pub fn portfolio() -> Html {
    html! {
        <section id="portfolio" class="portfolioSection">
        {"Portfolio Elements"}
        </section>
    }
}
